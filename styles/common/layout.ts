import { StyleSheet } from 'react-native';

export const layout = StyleSheet.create({
  fullScreen: {
    flex: 1,
  },
  centerContent: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});